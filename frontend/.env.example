# Frontend — echo-slice settings. Copy to .env to override defaults.
HOST=0.0.0.0
PORT=3000
# Where the browser opens its audio WebSocket (the gateway echo endpoint).
PUBLIC_WS_URL=ws://localhost:3001/ws/voice
